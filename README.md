# llamacpp_docker_6700xt

Local workspace for running `llama.cpp` with Docker and preloaded GGUF model files.

## Files

- `Dockerfile`
- `docker-compose.yml`
- `models/`

## Git

This repository was initialized locally in this workspace.
